# MatchSphere Quick Start Guide

## 🚀 Quick Installation (Copy & Paste)

Open PowerShell in your project directory and run these commands:

```powershell
# Navigate to project
cd "d:\ \Lab Assignment"

# Install all dependencies
npm install

# Install additional packages
npm install date-fns @react-native-community/netinfo

# Start the app
npm start
```

## 📱 Run on Your Phone

1. Install **Expo Go** app on your phone (App Store/Google Play)
2. Scan the QR code shown in terminal
3. Wait for app to load

## 🔐 Test Login

Use these credentials:

- **Username**: `emilys`
- **Password**: `emilyspass`

Or tap "Sign Up" to create a new account.

## 🎨 Features to Try

1. ✅ **Home Tab** - View today's matches
2. ✅ **Leagues Tab** - Browse available leagues
3. ✅ **Players Tab** - Search for players
4. ✅ **Favourites Tab** - Save your favorites
5. ✅ **Profile Tab** - Toggle dark mode

## ⚡ Development Commands

```powershell
npm start          # Start Expo
npm run android    # Run on Android
npm run ios        # Run on iOS (macOS only)
npm run web        # Run in browser
```

## 🐛 Troubleshooting

**Metro bundler error?**

```powershell
npx expo start -c
```

**Dependencies issue?**

```powershell
rm -rf node_modules
npm install
```

**Font error?**
The app will work with system fonts. Fonts are optional for testing.

## 📚 Full Documentation

See `README.md` and `INSTALLATION.md` for complete documentation.

---

**That's it! You're ready to explore MatchSphere! 🎉**
